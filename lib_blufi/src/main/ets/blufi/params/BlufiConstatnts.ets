export enum OpMode {
  MODE_NULL = 0,
  MODE_STA = 1,
  MODE_SOFTAP = 2,
  MODE_STASOFTAP = 3,
}

export enum DataDirection {
  OUTPUT = 0,
  INPUT = 1,
}

export enum SoftApSecurity {
  OPEN = 0,
  WEP = 1,
  WPA = 2,
  WPA2 = 3,
  WPA_WPA2 = 4,
}

export enum StaConnectionStatus {
  CONN_SUCCESS = 0,
  CONN_FAIL = 1,
  CONN_CONNECTING = 2,
  CONN_NO_IP = 3,
}

export enum ConnectionEndReason {
  WIFI_REASON_4WAY_HANDSHAKE_TIMEOUT = 15,
  WIFI_REASON_NO_AP_FOUND = 201,
  WIFI_REASON_HANDSHAKE_TIMEOUT = 204,
  WIFI_REASON_CONNECTION_FAIL = 205
}

export enum PackageType {
  CTRL = 0,
  DATA = 1,
}

export enum CtrlSubType {
  ACK = 0,
  SET_SEC_MODE = 1,
  SET_OP_MODE = 2,
  CONNECT_WIFI = 3,
  DISCONNECT_WIFI = 4,
  GET_WIFI_STATUS = 5,
  DEAUTHENTICATE = 6,
  GET_VERSION = 7,
  CLOSE_CONNECTION = 8,
  GET_WIFI_LIST = 9,
}

export enum DataSubType {
  NEGOTIATE = 0,
  STA_WIFI_BSSID = 1,
  STA_WIFI_SSID = 2,
  STA_WIFI_PASSWORD = 3,
  SOFTAP_WIFI_SSID = 4,
  SOFTAP_WIFI_PASSWORD = 5,
  SOFTAP_MAX_CONNECTION_COUNT = 6,
  SOFTAP_AUTH_MODE = 7,
  SOFTAP_CHANNEL = 8,
  USERNAME = 9,
  CA_CERTIFICATION = 10,
  CLIENT_CERTIFICATION = 11,
  SERVER_CERTIFICATION = 12,
  CLIENT_PRIVATE_KEY = 13,
  SERVER_PRIVATE_KEY = 14,
  WIFI_CONNECTION_STATE = 15,
  VERSION = 16,
  WIFI_LIST = 17,
  ERROR = 18,
  CUSTOM_DATA = 19,
  STA_WIFI_MAX_CONN_RETRY = 20,
  WIFI_CONN_END_REASON = 21,
  STA_WIFI_CONN_RSSI = 22,
}

export class BlufiUUIDs {
  public static readonly UUID_SERVICE = "0000FFFF-0000-1000-8000-00805F9B34FB"
  public static readonly UUID_WRITE_CHARACTERISTIC = "0000FF01-0000-1000-8000-00805F9B34FB"
  public static readonly UUID_NOTIFICATION_CHARACTERISTIC = "0000FF02-0000-1000-8000-00805F9B34FB"
  public static readonly UUID_NOTIFICATION_DESCRIPTOR = "00002902-0000-1000-8000-00805F9B34FB"
}

export enum BlufiCode {
  STATUS_SUCCESS = 0,
  WIFI_SCAN_FAIL = 11,
  INVALID_NOTIFICATION = -1000,
  WRITE_DATA_FAILED = -1002,
  INVALID_DATA = -1003,
  NEG_ERR_POST_FAILED = -2000,
  NEG_ERR_DEVICE_KEY = -2001,
  NEG_ERR_SECURITY = -2002,
  NEG_ERR_SET_SECURITY = -2003,
  CONF_INVALID_OPMODE = -3000,
  CONF_ERR_SET_OPMODE = -3001,
  CONF_ERR_POST_STA = -3002,
  CONF_ERROR_POST_SOFTAP = -3003,
  GATT_DISCOVER_SERVICE_FAILED = -4001,
  GATT_DISCOVER_WRITE_CHAR_FAILED = -4002,
  GATT_DISCOVER_NOTIFY_CHAR_FAILED = -4003,
  GATT_ERRO_OPEN_NOTIFY = -4004,
  GATT_CONNECT_FAILED = -4100,
}
